# Artefactos de build (generados, no versionar)
/build/
*.zip
*.tar.gz

# Scripts de prueba locales (no forman parte del módulo desplegable)
/test/

# Otros temporales que no deben subir
*.log
*.tmp
Thumbs.db
.DS_Store
